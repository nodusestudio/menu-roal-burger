{
  "name": "menu-roal-burger",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-images.js && terser src/script-v2.js -o script-v2.js -c passes=2 -m && terser src/admin.js -o admin.js -c passes=2 -m && terser src/tracking.js -o tracking.js -c -m && cleancss -o style.css src/style.css",
    "dev": "node -e \"['admin.js','script-v2.js','style.css','tracking.js'].forEach(f=>{require('fs').copyFileSync('src/'+f,f);console.log('copied src/'+f+' -> '+f)})\""
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "sharp": "^0.35.1",
    "terser": "^5.31.6"
  }
}
